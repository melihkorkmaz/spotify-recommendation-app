import reducer from './reducers';

export { default as tracksSelectors } from './selectors';
export { default as tracksOperations } from './operations';
export { default as tracksTypes } from './types';

export default reducer;
